export enum InvestorType {
    INDIVIDUAL = "individual",
    FIRM = "firm"
}

export enum PromptMessage {
    SUCCESS = "success",
    WARNING = "warning",
    ERROR = "error"
}

export enum FileType {
    IMAGE = "image",
    PDF = "pdf"
}